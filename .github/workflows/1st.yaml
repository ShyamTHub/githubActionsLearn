---
name: "hello to main branch"
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Hello to main commit"
        run: echo "Hello to main commit"
    